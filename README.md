# Travel Exchange Bot 🤖✈️

Telegram-бот для отслеживания расходов во время путешествий с автоматической конвертацией валют.

## Описание

Travel Exchange Bot — это миникошелек для путешественников, который помогает отслеживать расходы в разных валютах. Бот автоматически конвертирует суммы из валюты страны пребывания в домашнюю валюту и ведет учет баланса.

### Основные возможности

- ✈️ **Создание путешествий** — создавайте несколько путешествий с разными валютными парами
- 💱 **Автоматическая конвертация** — бот автоматически конвертирует расходы через API exchangerate.host
- 💰 **Учет баланса** — отслеживайте остаток средств в двух валютах
- 📊 **История расходов** — просматривайте все учтенные расходы
- 🔄 **Переключение между путешествиями** — легко переключайтесь между активными путешествиями
- 💵 **Ручной ввод курса** — возможность установить курс обмена вручную (например, по местному обменнику)

## Установка

### Требования

- Python 3.10 или выше
- Telegram Bot Token
- API ключ exchangerate.host (опционально, для бесплатного использования не требуется)

### Шаги установки

1. **Клонируйте репозиторий или скачайте файлы проекта**

2. **Создайте виртуальное окружение** (рекомендуется):
```bash
python -m venv venv
```

3. **Активируйте виртуальное окружение**:
   - Windows:
   ```bash
   venv\Scripts\activate
   ```
   - Linux/Mac:
   ```bash
   source venv/bin/activate
   ```

4. **Установите зависимости**:
```bash
pip install -r requiements.txt
```

## Настройка

### Переменные окружения

Создайте файл `.env` в корневой директории проекта (файл уже должен существовать, но проверьте его содержимое):

```env
# Telegram Bot Token (обязательно)
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here

# API ключ exchangerate.host (опционально, для бесплатного использования не требуется)
CURRENCY_API_KEY=your_api_key_here

# URL API (опционально, по умолчанию используется exchangerate.host)
CURRENCY_API_URL=http://api.exchangerate.host/convert
```

### Получение Telegram Bot Token

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте полученный токен в файл `.env`

### Получение API ключа exchangerate.host (опционально)

1. Перейдите на [exchangerate.host](https://exchangerate.host/)
2. Зарегистрируйтесь и получите бесплатный API ключ
3. Добавьте ключ в файл `.env`

**Примечание:** Бот может работать и без API ключа, используя бесплатный доступ к API exchangerate.host.

## Запуск

После настройки переменных окружения запустите бота:

```bash
python bot.py
```

Вы должны увидеть сообщение:
```
🤖 Бот запущен...
```

## Использование

### Команды бота

- `/start` — запустить бота и показать главное меню
- `/newtrip` — создать новое путешествие
- `/switch` — переключиться между путешествиями
- `/balance` — показать текущий баланс
- `/history` — показать историю расходов
- `/setrate` — изменить курс обмена
- `/cancel` — отменить текущую операцию

### Создание путешествия

1. Нажмите кнопку **"➕ Создать новое путешествие"** или отправьте `/newtrip`
2. Введите страну отправления (например: Россия)
3. Введите страну назначения (например: Китай)
4. Бот автоматически определит валюты и получит текущий курс обмена
5. Подтвердите курс или введите его вручную
6. Введите начальную сумму в валюте страны пребывания (или пропустите, чтобы начать с 0)

### Учет расходов

Просто отправьте боту число — он автоматически распознает его как расход в валюте страны пребывания:

1. Введите сумму (например: `100`)
2. Бот покажет конвертацию: `100.00 CNY = 800.00 RUB`
3. Нажмите **"✅ Да"** для учета расхода или **"❌ Нет"** для отмены
4. Расход будет вычтен из баланса

### Главное меню

Бот имеет удобное inline-меню с кнопками:

- **➕ Создать новое путешествие** — создать новое путешествие
- **✈️ Мои путешествия** — просмотреть все путешествия и переключиться между ними
- **💰 Баланс** — показать текущий баланс активного путешествия
- **📊 История расходов** — просмотреть последние 10 расходов
- **💱 Изменить курс** — обновить курс обмена для активного путешествия

## Структура проекта

```
Travel_Exchange/
├── bot.py              # Основной файл бота
├── database.py         # Модуль работы с базой данных SQLite
├── currency_api.py     # Модуль работы с API exchangerate.host
├── main.py             # Вспомогательные функции для HTTP-запросов
├── requiements.txt     # Список зависимостей
├── .env                # Файл с переменными окружения (не в репозитории)
├── travel_wallet.db    # База данных SQLite (создается автоматически)
└── README.md           # Этот файл
```

## База данных

Бот использует SQLite для хранения данных локально. База данных создается автоматически при первом запуске.

### Таблицы

- **trips** — информация о путешествиях (страны, валюты, курс, баланс)
- **expenses** — история расходов

Каждый пользователь имеет свой собственный набор путешествий и расходов.

## API

Бот использует [exchangerate.host](https://exchangerate.host/) для получения курсов валют и конвертации.

### Endpoints

- `http://api.exchangerate.host/convert` — конвертация валют
- `http://api.exchangerate.host/live` — получение текущих курсов
- `http://api.exchangerate.host/list` — список поддерживаемых валют

## Поддерживаемые валюты

Бот поддерживает более 160 валют, включая:
- Основные валюты: USD, EUR, GBP, JPY, CNY, RUB и др.
- Криптовалюты: BTC
- Драгоценные металлы: XAU (золото), XAG (серебро)

Полный список доступен через функцию `get_supported_currencies()` в `currency_api.py`.

## Обработка ошибок

Бот корректно обрабатывает следующие ситуации:

- ❌ Ошибки API (недоступность сервиса, неправильный ключ)
- ❌ Неверный ввод пользователя (не число, отрицательные значения)
- ❌ Отсутствие активного путешествия
- ❌ Неподдерживаемые валюты

Во всех случаях пользователь получает понятное сообщение об ошибке.

## Примеры использования

### Пример 1: Создание путешествия

```
Пользователь: /start
Бот: [Показывает главное меню]

Пользователь: [Нажимает "Создать новое путешествие"]
Бот: Введите страну отправления (например: Россия)

Пользователь: Россия
Бот: Введите страну назначения (например: Китай)

Пользователь: Китай
Бот: 📊 Текущий курс обмена:
     1 RUB = 0.088035 CNY
     Этот курс подходит?

Пользователь: [Нажимает "✅ Да, подходит"]
Бот: Введите начальную сумму в валюте RUB (например: 1000):

Пользователь: 10000
Бот: ✅ Путешествие создано!
     📍 Россия → Китай
     💱 Курс: 1 RUB = 0.088035 CNY
     💰 Начальный баланс: 10000.00 RUB = 880.35 CNY
```

### Пример 2: Учет расхода

```
Пользователь: 100
Бот: 💸 100.00 CNY = 1136.36 RUB
     Учесть как расход?
     [✅ Да] [❌ Нет]

Пользователь: [Нажимает "✅ Да"]
Бот: ✅ Расход учтен!
     💸 100.00 CNY = 1136.36 RUB
     Остаток: 10000.00 RUB = 880.35 CNY
```

## Разработка

### Зависимости

- `python-telegram-bot` — библиотека для работы с Telegram Bot API
- `requests` — для HTTP-запросов к API
- `python-dotenv` — для загрузки переменных окружения
- `colorama` — для цветного вывода в консоль (опционально)

### Расширение функционала

Для добавления новых функций:

1. Добавьте обработчики в `bot.py`
2. При необходимости расширьте схему базы данных в `database.py`
3. Добавьте новые функции API в `currency_api.py`

## Лицензия

Этот проект создан в образовательных целях.

## Поддержка

При возникновении проблем:

1. Проверьте, что все зависимости установлены
2. Убедитесь, что файл `.env` настроен правильно
3. Проверьте логи бота в консоли
4. Убедитесь, что у бота есть доступ к интернету для работы с API

## Автор

Проект создан в рамках обучения программированию.

---

**Приятных путешествий! ✈️💰**

